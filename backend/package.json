{
  "name": "clearview-audio-backend",
  "version": "1.0.0",
  "description": "Streams the best audio-only stream of a YouTube video to the ClearView Android app (ytdl-core primary, yt-dlp fallback).",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.3",
    "express": "^4.21.2",
    "youtube-dl-exec": "^3.1.10"
  }
}
