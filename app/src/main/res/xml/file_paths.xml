<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider paths: only the app's own cache dir is ever shared (the
     generated Progress Card PNGs) — no external or database paths. -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="shared_images" path="." />
</paths>
