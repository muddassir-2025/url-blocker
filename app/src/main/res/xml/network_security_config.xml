<?xml version="1.0" encoding="utf-8"?>
<!--
  ClearView backend runs over plain HTTP during development (localhost on the
  emulator host / a LAN IP). Cleartext is permitted ONLY for local dev hosts
  so the shared-rules sync works; all other traffic stays HTTPS-only.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
