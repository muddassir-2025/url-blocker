[versions]
agp = "9.3.1"
coreKtx = "1.19.0"
junit = "4.13.2"
junitVersion = "1.3.0"
espressoCore = "3.7.0"
lifecycleRuntimeKtx = "2.11.0"
activityCompose = "1.13.0"
kotlin = "2.2.10"
composeBom = "2026.02.01"
work = "2.10.0"
coroutines = "1.10.1"
orgJson = "20240303"
# Google AI Edge LiteRT — the official successor to TensorFlow Lite.
# Chosen over org.tensorflow:tensorflow-lite because:
#  - 16 KB page-size aligned native libs (required on Android 15+/16 KB
#    devices; tensorflow-lite <= 2.15 is not aligned and crashes at load).
#  - Modern runtime that loads models exported by recent TensorFlow.
#  - Keeps the org.tensorflow.lite.* Java packages, so the analyzer's
#    imports (org.tensorflow.lite.Interpreter / DataType) still work.
# Both litert and litert-api declare namespace "com.google.ai.edge.litert"
# (same duplicate-namespace issue as tensorflow-lite 2.7+), which is why
# gradle.properties sets android.uniquePackageNames=false.
litert = "2.1.6"
# NewPipeExtractor — on-device YouTube audio-stream extraction (audio-only, so
# no FFmpeg/merge step). GPL-3.0-or-later. Published on JitPack only.
newpipeExtractor = "v0.26.4"
# androidx.media — MediaSessionCompat + NotificationCompat.MediaStyle for the
# offline-audio foreground service (background playback with lock-screen and
# notification media controls).
media = "1.8.0"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "work" }
kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }
org-json = { group = "org.json", name = "json", version.ref = "orgJson" }
litert = { group = "com.google.ai.edge.litert", name = "litert", version.ref = "litert" }
newpipe-extractor = { group = "com.github.TeamNewPipe", name = "NewPipeExtractor", version.ref = "newpipeExtractor" }
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }
androidx-media = { group = "androidx.media", name = "media", version.ref = "media" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycleRuntimeKtx" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycleRuntimeKtx" }
androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycleRuntimeKtx" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-compose-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }

